/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package mdsimulation;

/**
 *
 * @author Ian
 */
public class MiscUnusedCode
{
    
//    /* Checks for collsion with Vertical (xCoordinate) wall */
//    public double timeWColX(Particle par)
//    {
//        double deltaT = 0;
//        
//        if(par.getVX() > 0)
//            {deltaT = (1 - par.getRadius() - par.getRX()) / par.getVX();}
//        else if (par.getVX() < 0)
//            {deltaT = (par.getRadius() - par.getRX()) / par.getVX();}
//        else if(par.getVX() == 0)
//            {deltaT = NO_COLLISION;}        
//        return deltaT;
//    }
//    /* Checks for collison with Horizontal (yCoordinate) wall*/
//    public double timeWColY(Particle par)
//    {
//        double deltaT = 0;
//        
//        if(par.getVY() > 0)
//            {deltaT = (1 - par.getRadius() - par.getRY()) / par.getVY();}
//        else if (par.getVY() < 0)
//            {deltaT = (par.getRadius() - par.getRY()) / par.getVY();}
//        else if(par.getVY() == 0)
//            {deltaT = NO_COLLISION;}        
//        return deltaT;
//    }
}
